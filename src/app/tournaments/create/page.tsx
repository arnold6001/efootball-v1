// ... add fields
const [format, setFormat] = useState('single-elim'); // or 'round-robin'
const [teamBased, setTeamBased] = useState(false);
const [prize, setPrize] = useState('');

// In tournament object:
format,
teamBased,
prize,